export interface IListEyewearRes {
  id: number;
  name: string;
  description: string;
  price: number;
  maxQuantity: number;
  code: string;
  thumbnail: string;
}

export interface IColor {
  id: number;
  name: string;
  code: string;
}
